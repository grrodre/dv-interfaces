# Changelog

## [2.0.1] — 2026-06-22

Fix absolute image URLs in README for correct rendering on PyPI.

## [2.0.0] — 2026-06-21

Initial public release. Four drivers (SolarLog, SMA, Meteocontrol, SmartDog), unified `read_dataset()` API, write API (`turn_on`, `turn_off`, `limit_plant`), auto-reconnect, `detect_interface()` auto-detection, `stream()` generator.
